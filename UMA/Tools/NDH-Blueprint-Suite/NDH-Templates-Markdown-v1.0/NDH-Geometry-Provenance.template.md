# NDH Geometry Provenance — {INSTANCE_LABEL}

## 1. Base Object Definition
The Non-Dual Hexeract (NDH) is derived from a classical 6-cube (hexeract) with axes:

- x1 → {AXIS_X1_LABEL}
- x2 → {AXIS_X2_LABEL}
- x3 → {AXIS_X3_LABEL}
- x4 → {AXIS_X4_LABEL}
- x5 → {AXIS_X5_LABEL}
- x6 → {AXIS_X6_LABEL}

This mapping is mandatory for all NDH artifacts.

## 2. Non-Dual Collapse
The NDH is defined by the collapse condition:

```
{AXIS_X1_LABEL} = {AXIS_X2_LABEL} = {AXIS_X3_LABEL} = {AXIS_X4_LABEL} = {AXIS_X5_LABEL} = {AXIS_X6_LABEL}
```

This collapse is visually expressed by:
- a central node labeled `{CENTER_NODE_LABEL}`
- faded axis labels
- unified dimensional identity

## 3. SGE Monad
The SGE monad is defined as:

```
{MONAD_LABEL} : {CENTER_NODE_LABEL} → {CENTER_NODE_LABEL}
```

The monad layer must include:
- a closed loop
- a directional arrow
- a monad label

## 4. Epoch Ω Embedding
Epoch Ω is represented as:

```
{OMEGA_MAP_LABEL} : {MONAD_LABEL}({CENTER_NODE_LABEL}) → {CENTER_NODE_LABEL}
```

The Ω layer must include:
- a dashed ring labeled `{OMEGA_LABEL}`
- an inward arrow labeled `{OMEGA_MAP_LABEL}`

## 5. Adjacency & Resonance Lattice
The NDH adjacency/resonance lattice must include:
- four radial adjacency nodes
- four adjacency edges
- four resonance arcs
- a central awareness node `{CENTER_NODE_LABEL}`

This expresses 6D adjacency/resonance behavior in 2D projection.

## 6. CHS Integration
The CHS → NDH dimensional upgrade is defined as:

```
CHS (5D) + {AWARENESS_AXIS_LABEL} → NDH (6D)
```

The integration map must include:
- CHS block labeled `{CHS_LABEL}`
- NDH block labeled `{NDH_LABEL}`
- an arrow labeled `{AWARENESS_AXIS_LABEL}`

## 7. Linked SVG Artifacts
This provenance document must link to the following generated SVGs:

- NDH-Hexeract-Base-6D.svg
- NDH-NonDual-Collapse-Overlay.svg
- NDH-SGE-Monad-Flow.svg
- NDH-Epoch-Omega-Embedding.svg
- NDH-Adjacency-Resonance-Lattice.svg
- NDH-CHS-Integration-Map.svg

## 8. Version
Version: {VERSION_LABEL}
Status: Generated from NDH-Blueprint-Spec-v1.0
```

---

